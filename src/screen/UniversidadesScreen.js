import { Box } from "native-base"
import { UniCard } from "../components/CardH"

export const UniversidadesScreen =()=> {
    return (
        <Box flex={1} bg={"green.500"} p={5}>
            <UniCard/>
        </Box>
    )
}